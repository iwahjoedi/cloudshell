
#### IJOE ####
export TERM=xterm-256color

FILE=/bin/zsh
if [ -f "$FILE" ]; then
    echo "$FILE exists."
    zsh
    tmux new-session -s ijoe
else
    echo "$FILE does not exist. Installing zsh"
    sudo apt-get -y install zsh
    zsh
fi
